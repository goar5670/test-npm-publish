name: README Spell Check

on: [push]

jobs:
  Spell-Check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Aspell
        run: sudo apt-get update && sudo apt-get install -y aspell aspell-en
      - name: Spell Check
        run: aspell -c README.md