name: README Spell Check

on: [push]

jobs:
  Spell-Check:
    runs-on: ubuntu-latest
    steps:
      - name: Spell Check 📃
        uses: mh61503891/action-paper-aspell@master
        env:
          ASPELL_LANG: en
          fASPELL_MODE: markdown
          ASPELL_FILES: /README.md